# Portfólio — João Franco

React + Vite + Tailwind v4.

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # gera dist/
```

Edite todo o conteúdo em `src/data.js`. Deploy: Vercel (importar repo, zero config) ou GitHub Pages (`vite.config.js` → `base: '/repo/'`).
